# LeetCode.cn cookie（从浏览器复制，建议仅包含必要字段）
# 示例：LEETCODE_SESSION=xxxx; csrftoken=yyyy
LEETCODE_COOKIE=

# 可选：默认语言（python3 / cpp / java 等）
DEFAULT_LANG=python3

# 可选：默认每次拉取题目数量
DEFAULT_LIMIT=10

# OpenAI API（用于 ai-fallback）
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.3
